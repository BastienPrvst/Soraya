
<turbo-frame id="delivery_infos">
    {{ form_start(form, {
        action: path('checkout_delivery', { token: order.token }),
        method: 'POST',
        attr: {
            'data-turbo': 'false'
        }
    }) }}

    {{ form_widget(form) }}

    {{ form_end(form) }}
</turbo-frame>
